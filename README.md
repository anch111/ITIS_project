# 🎓 Educational Plagiarism Detector  
**Итоговый проект по ИТИС | Зенкина Анна, РИ-150931**

![Python 3.9+](https://img.shields.io/badge/python-3.9%2B-blue)
![License: MIT](https://img.shields.io/badge/license-MIT-green)
![CI: passing](https://img.shields.io/badge/CI-passing-brightgreen)

---

## 🧠 Зачем это нужно?

Сегодня всё больше студентов прибегают к некритическому копированию материалов из интернета. Это ставит под угрозу качество образования и объективность оценок.  

Ручная проверка на плагиат — трудоёмка и субъективна.  
**Мой проект** предлагает автоматизированное, объективное и прозрачное решение для выявления текстовых заимствований на основе проверенных NLP-методов.

> 💡 Проект создан **с нуля** и направлен на реальное применение в учебных заведениях.

---

## 🌟 Ключевые возможности

- 🔍 **Поддержка форматов**: `.txt` и `.pdf`
- 🧹 **Предобработка текста**: токенизация, лемматизация, удаление стоп-слов
- 📊 **Три метода сравнения**:
  - **Косинусное сходство + TF-IDF** — для взвешенного сравнения важности слов
  - **LCS (Longest Common Subsequence)** — для поиска совпадающих фраз
  - **N-gram matching** — для сравнения локальных последовательностей
- 📈 **Визуализация**: тепловая матрица сходства между всеми парами текстов
- 📤 **Экспорт результатов**: отчёт сохраняется в `JSON` с временной меткой

---

## 📂 Структура проекта

- `src/` — основной код
  - `detector.py` — основной класс анализа
  - `preprocessor.py` — очистка текста
  - `similarity.py` — алгоритмы сравнения
  - `utils.py` — вспомогательные функции
- `tests/` — unit-тесты
- `data/` — примеры учебных работ
- `uploads/` — папка для загрузки новых работ (для CI/CD)
- `reports/` — автоматически генерируемые отчёты
- `examples/demo.py` — пример запуска
- `requirements.txt`, `.gitignore`, `LICENSE`, `pytest.ini`, `README.md`

---

## ⚙️ Установка

### Требования
- Python 3.9 или новее
- `pip`

### Пошагово
```bash
# 1. Клонируйте репозиторий
git clone https://github.com/ваш-логин/plagiarism-detector.git
cd plagiarism-detector

# 2. Виртуальное окружение (рекомендуется)
python -m venv env
source env/bin/activate      # Linux/macOS
# env\Scripts\activate       # Windows

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Загрузите языковые модели NLTK
python -c "import nltk; nltk.download('punkt', 'stopwords', 'wordnet')"
```

## ▶️ Пример использования
```python
from src.detector import PlagiarismDetector

# Примеры студенческих работ
essays = [
    "Биология изучает живые организмы и их взаимодействие с окружающей средой.",
    "Биология — это наука, которая исследует живые организмы и их связь с окружающим миром.",
    "Математика — царица наук, логика и точность в одном целом."
]

# Запуск анализа
detector = PlagiarismDetector(threshold=0.65)
matches = detector.detect_plagiarism(essays)

print(f"Найдено {len(matches)} подозрительных пар(ы):")
for i, j, score in matches:
    print(f" • Работа {i+1} ↔ Работа {j+1} (сходство: {score:.1%})")
```

### Результат выполнения:
```
Найдено 1 подозрительных пар(ы):
 • Работа 1 ↔ Работа 2 (сходство: 83.2%)
```

## 🧪 Тестирование

Проект включает автоматизированные тесты:
```bash
pytest tests/ -v
```
